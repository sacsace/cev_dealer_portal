# Railway config for the **web** service.
# Service Settings → Config-as-code → railway.web.toml

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.web"

[deploy]
startCommand = "npm run start --workspace=web"

[variables]
NPM_CONFIG_INCLUDE = "optional"
