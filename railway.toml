[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.web"

[deploy]
startCommand = "npm run start --workspace=web"

# Remove NPM_CONFIG_OMIT from Railway Variables if you still see omit="" warnings.

[variables]
NPM_CONFIG_INCLUDE = "optional"
