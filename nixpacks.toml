[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = [
  "npm ci --include=dev --include=optional"
]

[phases.build]
cmds = [
  "npm run build --workspace=web"
]

[start]
cmd = "npm run start --workspace=web"
